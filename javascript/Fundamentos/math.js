const raio = 5.6
const area = Math.PI + Math.pow(raio, 2)

/**
 * O Math é um objeto
 * PI é uma constante dentro de Math
 * POW é uma função que calcula a potencia
 */
console.log(area.toFixed(2))
console.log(typeof Math)